FEEDBACK PIPELINE SUMMARY — 2025-08-28

This is a safe copy of the feedback pipeline summary placed under `docs/` with a filename that avoids common ignore patterns.

Summary:
- New feedback proto and generated Go bindings added.
- Collector and aggregator scaffolding implemented.
- Deterministic proto regen tooling added and many `.pb.go` files reorganized.
- Ongoing fixes: `cmd/vector-consumer-v2` needs proto accessor and cache callsite updates.

This file is intended to be committed to the repository under `docs/`.
