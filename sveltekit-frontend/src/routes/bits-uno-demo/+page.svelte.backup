<script lang="ts">
  import BitsUnoDemo from "$lib/components/ui/BitsUnoDemo.svelte";
  import { goto } from '$app/navigation';
</script>

<svelte:head>
  <title>Bits UI + UnoCSS Demo | Legal Case Management</title>
  <meta name="description" content="Demonstration of Bits UI and UnoCSS integration in the legal case management system." />
</svelte:head>

<!-- Navigation Header -->
<div className="${1}">
  <div className="${1}">
    <div className="${1}">
      <div className="${1}">
        <button 
          on:click={() => goto('/')}
          className="${1}"
        >
          ← Back to Dashboard
        </button>
      </div>
      <h1 className="${1}">UI Integration Demo</h1>
      <div className="${1}"></div> <!-- Spacer for centering -->
    </div>
  </div>
</div>

<!-- Demo Component -->
<BitsUnoDemo />

<!-- Additional Information -->
<div className="${1}">
  <div className="${1}">
    <div className="${1}">
      <h2 className="${1}">Technical Implementation</h2>
      
      <div className="${1}">
        <div>
          <h3 className="${1}">Installed Packages</h3>
          <div className="${1}">
            <div className="${1}">
              <div>bits-ui@2.8.10</div>
              <div>unocss@66.3.2</div>
              <div>@unocss/vite@66.3.2</div>
              <div>@unocss/preset-uno@66.3.2</div>
              <div>@unocss/preset-attributify@66.3.2</div>
              <div>@unocss/preset-icons@66.3.2</div>
              <div>@unocss/preset-typography@66.3.2</div>
              <div>@unocss/preset-web-fonts@66.3.2</div>
            </div>
          </div>
        </div>
        
        <div>
          <h3 className="${1}">Configuration Files</h3>
          <div className="${1}">
            <div className="${1}">
              <span className="${1}">✓</span>
              <code className="${1}">unocss.config.ts</code>
            </div>
            <div className="${1}">
              <span className="${1}">✓</span>
              <code className="${1}">vite.config.ts</code>
            </div>
            <div className="${1}">
              <span className="${1}">✓</span>
              <code className="${1}">+layout.svelte</code>
            </div>
            <div className="${1}">
              <span className="${1}">✓</span>
              <code className="${1}">package.json</code>
            </div>
          </div>
        </div>
      </div>
      
      <div className="${1}">
        <h4 className="${1}">Integration Status</h4>
        <p className="${1}">
          ✅ Both Bits UI and UnoCSS are successfully integrated and working together in both the web-app and desktop-app (which shares the same frontend).
          The desktop app uses the web-app's SvelteKit frontend as specified in the Tauri configuration.
        </p>
      </div>
    </div>
  </div>
</div>
