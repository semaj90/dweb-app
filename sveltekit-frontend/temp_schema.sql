CREATE TABLE IF NOT EXISTS document_metadata (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), case_id uuid, original_filename varchar(512) NOT NULL, summary text, content_type varchar(100), file_size integer, processing_status varchar(50) DEFAULT 'pending', metadata jsonb DEFAULT '{}' NOT NULL, created_at timestamp DEFAULT now() NOT NULL, updated_at timestamp DEFAULT now() NOT NULL);
