# Dockerfile for LangChain API service
FROM python:3.11-slim

WORKDIR /app

# Install Python dependencies
COPY requirements.txt .
RUN pip install -r requirements.txt

# Copy LangChain service files
COPY langchain-service.py .

EXPOSE 8000

CMD ["python", "langchain-service.py"]
