package main

// Windows build stub for cuda-service. The enhanced CUDA server is behind the 'enhanced' build tag.
// This stub prevents empty file parse errors on Windows builds when the enhanced tag isn't used.

func main() {}
